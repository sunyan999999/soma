# TEST_REPORT — SOMA v2.0.11

> 生成日期：2026-08-22
> 版本：v2.0.11（向量索引一致性修复 + 严苛测试强化）

## 测试结果总览

| 轮次 | 通过 | 失败 | 耗时 |
|------|------|------|------|
| 全量回归 | 853 | 0 | 115~138s |
| 严苛 stress（第 1 遍） | 21 | 0 | 15.95s |
| 严苛 stress（第 2 遍） | 21 | 0 | 16.38s |
| 严苛 stress（第 3 遍） | 21 | 0 | 20.94s |
| 安全相关（rbac/plugin/router/knowledge_gate/external/capture） | 118 | 0 | 7.13s |

**全量 853 用例零回归，连续 3 遍 stress 零 flaky。红绿验证证明新增测试能抓 bug。**

## 测试环境

- Python 3.13.9
- pytest 8.4.2 + pluggy 1.5.0
- faiss 1.14.3 / fastembed 0.7.4 / litellm 1.96.x（锁定）
- 平台：Windows 11 Home China 10.0.26200
- 全 mock 模型（不连真实 LLM / 本地模型服务）

## 本次改动摘要

### 修复（生产故障根因）

**向量索引一致性重大修复** — 生产环境（2.0.9/2.0.10）记忆检索退化（recall 0.5 / query 300ms）的根因，经 git diff 证实为**存量 bug**（记忆链路 2.0.9→2.0.10 零代码改动）：

| 修复 | 说明 |
|------|------|
| `store_vector()` 缺 `import faiss` | 索引构建后每次插入记忆抛 NameError → DB 有向量但 faiss 索引不更新（失步）→ 新记忆语义检索永远搜不到 |
| `similarity_search()` 失步处理遗漏 | 只处理 `count>ntotal`，遗漏 `count<ntotal`（删除残留）→ 现改为 DB 与 faiss 不一致即从 DB 全量重建，自愈 |
| 「嵌入向量生成失败」日志误导 | 拆开 encode 与索引写入，分别记录真实异常，便于生产定位 |

### 新增测试

- `tests/test_vector_store.py` +3：索引构建后插入不失步 / 删除不残留 / 失步自愈
- `tests/test_vector_store_stress.py` +13 严苛场景：
  - 核心一致性循环（3 seed × 15 轮 = 45 轮插入/删除/查询）
  - 300 条大量插入不失步
  - 磁盘持久化重启一致 + 未存盘窗口重启重建补全
  - 损坏索引恢复 / 维度迁移
  - 并发插入+查询（4 writer + 1 reader）
  - embed/索引失败降级 + 自愈
  - 端到端多轮 benchmark 模拟召回稳定
  - 2000 条性能对照（一致快 / 失步重建 / 恢复）

## 验证方法论

1. **全量回归**：853 用例（含新增 16 个向量测试）
2. **稳定性**：连续 3 遍 stress 测试，零 flaky
3. **红绿验证**：临时回退修复（移除 `import faiss`）→ 一致性测试**失败**（NameError 实锤）；恢复后**通过**——证明测试确实能抓 bug，非"全绿但测不到点"
4. **性能对照**：2000 条数据验证一致状态查询 <100ms，失步才触发重建，重建后恢复快速，不再出现 300ms 常态

## 边界审查

| 功能 | 审查项 | 结果 |
|------|--------|------|
| store_vector | 索引未构建 / 已构建 / 增量失败 / 大量连续插入 | 健壮 ✓ |
| delete_vector | 删除后残留不占用 top-k / 失步自愈 | 正常 ✓ |
| 持久化 | 磁盘索引加载 / 未存盘窗口 / 损坏文件 | 恢复 ✓ |
| 维度迁移 | 旧维度向量清除 / 新维度插入 | 正常 ✓ |
| 并发 | 多线程插入+查询 无崩溃 / 最终一致 | 通过 ✓ |
| 降级 | embed 失败记忆仍存 / 索引写入失败自愈 | 正常 ✓ |

## 安全审查

- 本次改动无新增输入面（无新 API / 用户输入 / 网络 / 文件 / 命令执行）
- 改动文件安全扫描：无 `os.system` / `subprocess` / `shell=True` / `eval` / `exec` / `pickle` / 非安全 yaml / 路径穿越
- 版本号一致性：pyproject = `__init__` fallback = `2.0.11` ✓
- 安全相关测试 118 通过（rbac 权限 / plugin / router / knowledge_gate / external / capture）
