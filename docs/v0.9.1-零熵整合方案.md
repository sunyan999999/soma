# SOMA v0.9.1 零熵智慧整合方案

> 版本定位: v0.9.0 里程碑升级 | 原则: 零接口变更 | 新增: 觉察维度

---

## 一、为什么是 v0.9.1

### 版本语义

v0.9.0 完成了「从个体到群体」的跃迁 — 多 Agent 协作、专家路由、共识协议已就位。本次整合在此之上新增一个维度：**觉察**。

不改变现有架构。不修改任何公开 API。六个模块像六块积木，按需叠加上去。

v0.9.1 = v0.9.0 的全部能力 + 「看见自己在怎么想」的觉察层。

### 与 v0.10.0 的关系

原计划 v0.10.0 的四个方向（Agent 自动孵化、跨 Agent 知识蒸馏、Agent 市场协议、联邦演化）**不受影响**。本次整合是中途插入的里程碑，不替代原有路线，可并行推进。

---

## 二、兼容性第一原则

### 2.1 硬约束

> **2000+ 开发者的存量代码，升级到 v0.9.1 后不做任何改动就能正常运行。**

这是最高优先级约束。具体体现为五条铁律：

| # | 铁律 | 验证方式 |
|---|------|---------|
| 1 | 所有公开方法的签名不变 | 逐个比对 `SOMA`、`SOMA_Agent`、`SOMAConfig`、`ConsensusProtocol` 等 30+ 个公开方法 |
| 2 | 所有公开类的构造参数不变 | 现有 `SOMA(llm="...", use_vector_search=True)` 写法不报错 |
| 3 | 所有公开方法的返回类型不变 | `respond()` 还是返回 `str`，`chat()` 还是返回 `dict` |
| 4 | 所有新行为默认关闭 | 新增 config 字段默认值 `False` / `None` / `0` |
| 5 | 新模块不影响已有 import | 新增 `soma.entropy`、`soma.hooks` 等包，不修改已有 import 路径 |

### 2.2 公开接口清单（基线）

以下 30 个公开接口在 v0.9.1 中**不做任何签名变更**：

```
SOMA.__init__(framework_config, llm, use_vector_search, persist_dir, recall_threshold, top_k, agent_id, group_id)
SOMA.respond(problem, user_id) → str
SOMA.chat(problem, user_id) → dict
SOMA.remember(content, context, importance, user_id, session_id) → str
SOMA.remember_semantic(subject, predicate, object_, confidence, namespace) → None
SOMA.query_memory(query, top_k, user_id) → list
SOMA.decompose(problem) → list
SOMA.reflect(task_id, outcome) → None
SOMA.evolve() → list
SOMA.get_weights() → dict
SOMA.adjust_weight(law_id, new_weight) → bool
SOMA.discover_laws() → dict|None
SOMA.approve_law(candidate) → bool
SOMA.get_thought_templates() → list
SOMA.close() → None
SOMA.stats → dict  (property)

SOMA_Agent.__init__(config, agent_id, group_id)
SOMA_Agent.respond(problem, user_id) → str
SOMA_Agent.chat(problem, user_id) → dict  (虽未直接在agent.py定义，但通过__init__.py暴露)

SOMAConfig(framework_path, episodic_persist_dir, llm_model, default_top_k, recall_threshold,
           adaptive_params, llm_cache_ttl, llm_cache_max_size, causal_extraction,
           causal_extraction_complexity, embedding_model_name, use_vector_search,
           vector_dim, embedder_device, framework)

ConsensusProtocol.__init__(llm_call)
ConsensusProtocol.form_consensus(question, opinions, strategy) → ConsensusResult

AgentRegistry.register(agent, expertise, description, is_default)
AgentRegistry.unregister(agent_id)
AgentRegistry.get(agent_id)
AgentRegistry.find_experts(domain, min_score)
ExpertRouter.route(problem, min_confidence, allow_fallback)
DistributedEvolver.register_agent(...) / evolve_all() / merge_weights(...)

MemoryUnit(id, content, timestamp, importance, access_count, context, memory_type,
           user_id, session_id, agent_id, shared_group_id)
```

### 2.3 新增内容如何做到不影响存量

```
新增方式                      │  对存量代码的影响
─────────────────────────────┼──────────────────────────
SOMAConfig 新增可选字段       │  Pydantic 自动设默认值，旧代码不传不报错
SOMA 新增方法                 │  加新方法不影响已有调用
soma.xxx 新子包               │  不 import 就不加载，零开销
管道步骤用 if config.flag: 包裹 │  flag 默认 False，旧代码路径完全跳过
新策略作为 ConsensusProtocol 选项 │  默认 strategy="auto"，行为不变
新 SQLite 表                  │  独立表名和前缀，不与现有表冲突
```

---

## 三、全局约束与设计准则

每个模块的设计必须满足以下 8 条准则：

**G1. 错误隔离**：任何新模块的异常不得传播到核心管道。所有新代码调用包裹在 `try/except` 中，记录日志后静默跳过。

**G2. 无嵌入器降级**：依赖 embedder 的功能必须有非嵌入器的回退路径（如字符级 bigram 相似度替代余弦相似度）。

**G3. 无 LLM 降级**：依赖 LLM 的功能必须有非 LLM 的回退路径（如规则替代仲裁）。

**G4. 可配置开关**：每个模块有独立的启用开关。用户可以只开某一个而关闭其他五个。

**G5. 资源上限**：任何新数据存储（SQLite 表、内存缓存）都有明确的大小上限和淘汰策略。

**G6. 线程安全**：在 WAL 模式下读写不互斥。Python 层的全局状态（如 hook registry）使用线程安全的数据结构。

**G7. 日志可观测**：每个模块的关键状态变化（启用/禁用/触发/异常）都输出 INFO/DEBUG 日志。

**G8. 独立可测试**：每个模块可以在 `SOMA(enable_xxx=False)` 环境下独立测试，不需要同时启用其他模块。

---

## 四、模块逐一拆解

以下每个模块按统一格式展开：
- 实现内容（精确到新增哪些文件、修改哪些文件的哪些位置）
- 兼容性保证（证明对存量零影响）
- 风险矩阵（每个步骤可能出什么错、概率、影响、缓解）

---

### 模块一：认知熵值仪表盘

**定位**：独立观察者模块。挂在管道末尾，不修改管道内任何逻辑。

#### 实现内容

**新增文件（3 个）**：

```
soma/entropy.py              (~220 行)
  └─ CognitiveEntropyCalculator 类
     ├─ calculate(conversation_turns) → dict
     ├─ _logic_coherence(turns) → float     # 相邻轮次余弦相似度均值
     ├─ _emotional_stability(turns) → float  # 负面词密度倒数
     ├─ _focus_clarity(turns) → float        # 话题向量方差倒数
     ├─ _load_negative_words() → set
     ├─ GuidanceState(Enum)                   # NORMAL / GUIDANCE
     ├─ _check_guidance_transition() → GuidanceState  # D2 确认的阈值逻辑
     └─ _render_breathing_reminder() → str            # D2 确认的呼吸提醒

soma/entropy/__init__.py     (空)
soma/entropy/negative_words.txt  (~150词，从零熵语料提取)
```

**修改文件（2 个，增量极小）**：

```
soma/config.py (+5 行)
  SOMAConfig 新增字段:
    enable_cognitive_entropy: bool = False  # 默认关闭
    entropy_window_size: int = 20            # 滑动窗口大小

soma/__init__.py (+15 行)
  SOMA.__init__ 中:
    if self._config.enable_cognitive_entropy:
        self._entropy_calc = CognitiveEntropyCalculator(...)
  
  SOMA 新增方法 (不影响存量):
    def get_cognitive_state(self, user_id: str = "") -> dict
```

#### 兼容性证明

| 检查项 | 结果 |
|--------|------|
| 现有方法签名是否改变 | 否 — 只新增 `get_cognitive_state()` |
| 现有方法返回值是否改变 | 否 — `respond()`/`chat()` 返回值不变 |
| 默认行为是否改变 | 否 — `enable_cognitive_entropy=False`，旧路径完全不动 |
| 新增 import 是否影响启动 | 否 — `soma.entropy` 只在 config flag 为 True 时才 import |
| 新增数据表是否冲突 | 否 — 熵值历史写入 `analytics` 表的新列 `entropy_score`（可 NULL） |

`soma/analytics.py` 的 `analytics_sessions` 表新增一列：
```sql
ALTER TABLE analytics_sessions ADD COLUMN entropy_score REAL DEFAULT NULL;
```
列有 DEFAULT NULL，存量行不受影响，SQLite ALTER TABLE 在表空时也是瞬间完成。

#### 风险矩阵

| # | 故障场景 | 触发条件 | 概率 | 影响 | 缓解措施 |
|---|---------|---------|------|------|---------|
| E1 | `negative_words.txt` 文件缺失 | 安装不完整 | 低 | 熵值计算失败 | `except FileNotFoundError` → 使用内置 50 词最小词表 |
| E2 | 旧版 analytics 表无 `entropy_score` 列 | 从 v0.9.0 升级 | 中 | 写入失败 | ALTER TABLE 用 try/except 包裹，列已存在则跳过 |
| E3 | 对话轮次不足（<3 轮） | 新用户 | 高 | 无法计算连贯性 | `len(turns) < 3` → 返回 `clarity_index=50, grade="数据不足"` |
| E4 | embedder 不可用 | 未安装 ONNX | 低 | 连贯性计算崩溃 | 回退到字符 bigram 重叠率（已有实现可复用） |
| E5 | 10000+ 轮对话后窗口计算变慢 | 长期用户 | 低 | 响应延迟 | 窗口固定 20 轮，O(20) 计算量恒定 |

#### 引导模式状态机（D2 确认参数）

```python
# 阈值参数（D2 确认）
GUIDANCE_ENTER_THRESHOLD = 40       # 熵值 < 40 分
GUIDANCE_ENTER_CONSECUTIVE = 3      # 连续 3 轮
GUIDANCE_EXIT_THRESHOLD = 50        # 熵值 ≥ 50 分
GUIDANCE_EXIT_CONSECUTIVE = 2       # 连续 2 轮
GUIDANCE_DIRECT_ANSWER_RATIO = 0.3  # 引导模式下直接回答占比上限

# 引导模式下的行为变更：
# 1. respond() 直接回答从 70%+ 降至 < 30%
# 2. 以引导性问题替代直接结论：「如果换个角度…？」「你觉得根源在哪里？」
# 3. 响应末尾追加呼吸提醒（随机从 3 条内置模板选取）

BREATHING_REMINDERS = [
    "在继续之前，不妨先做一次深呼吸，感受当下的状态。",
    "暂停三秒，问问自己：我现在的情绪在哪个位置？",
    "注意到思绪的速度了吗？先回到呼吸，再回到问题。",
]
```

引导模式状态存储在内存中（会话级临时状态，不持久化）。进入和退出均有 INFO 日志记录。

---

### 模块二：空性偏差检测升级

**定位**：现有偏差检测的追加式扩展。在 `ConflictDetector` 的调用点之后，追加一次 `FrameAnchoringDetector` 调用。

#### 实现内容

**新增文件（2 个）**：

```
soma/hub/_frame_detector.py  (~90 行)
  └─ FrameAnchoringDetector 类
     ├─ detect(conversation_turns) → Optional[dict]
     ├─ _load_frames() → dict
     └─ _count_frame_usage(turns) → dict

soma/hub/frame_keywords.yaml  (~60 行)
  8 对框架关键词 + 对立映射
```

**修改文件（3 个，追加式修改）**：

```
soma/config.py (+3 行)
  SOMAConfig 新增字段:
    enable_frame_detection: bool = False
    frame_detection_window: int = 5

soma/hub/_core.py (+8 行)
  ActivationHub.__init__ 中:
    self._frame_detector = None
    if enable_frame_detection:
        self._frame_detector = FrameAnchoringDetector()
  
  ActivationHub.activate() 末尾:
    不修改 activate() 本身。新增独立方法:
    def detect_frame_anchoring(self, recent_turns) → Optional[dict]

soma/agent.py (+10 行)
  _build_prompt() 中，在「反面视角与潜在矛盾」section 之后追加:
    if self.config.enable_frame_detection:
        frame_result = self.hub.detect_frame_anchoring(recent_turns)
        if frame_result:
            prompt_parts.append(f"\n## 觉察提示\n{frame_result['reflection']}")
```

#### 兼容性证明

| 检查项 | 结果 |
|--------|------|
| 现有方法签名是否改变 | 否 — `ActivationHub` 新增方法，不修改已有方法 |
| 默认行为是否改变 | 否 — `enable_frame_detection=False`，prompt 不追加任何新内容 |
| prompt 结构是否改变 | 否 — 只在 flag 为 True 时追加 section，flag 关闭时 prompt 与 v0.9.0 完全一致 |
| 框架关键词库缺失时是否崩溃 | 否 — `_load_frames()` 有内置 8 对的默认词库，YAML 文件缺失时回退 |

#### 风险矩阵

| # | 故障场景 | 触发条件 | 概率 | 影响 | 缓解措施 |
|---|---------|---------|------|------|---------|
| F1 | 框架检测过于频繁触发 | 用户高频使用同一框架合理 | 中 | 体验干扰 | 阈值设为 60%+（5 轮中 3 轮以上），且同一框架每小时只提示一次 |
| F2 | 关键词匹配误判框架 | 正常对话中包含框架词 | 低 | 错误提示 | 检测结果只在 prompt 中以温和建议形式出现，LLM 可自行判断是否采纳 |
| F3 | prompt 因追加内容超长 | 已有大量记忆和反视角时 | 低 | LLM token 超限 | 框架提示控制在 80 字以内 |
| F4 | `recent_turns` 数据不足 | 新会话 | 高 | 检测无结果 | `len(turns) < window_size` → 直接返回 None |
| F5 | 对立框架映射缺失 | 新增框架未定义 opposite | 低 | 提示不完整 | 用 `opposite_frame.get(frame, "另一个视角")` 兜底 |

---

### 模块三：内求复盘钩子

**定位**：管道的透明观察者层。在推理管道每步之后检查并生成觉察提示，但不改变任何步骤的输入输出。

#### 实现内容

**新增文件（5 个）**：

```
soma/hooks/__init__.py        (导出)
soma/hooks/_core.py           (~150 行)
  └─ InnerReflectionHook 数据类
  └─ HookRegistry 类
     ├─ load_builtin()
     ├─ load_from_config(yaml_path)
     ├─ match(step_name, context) → List[str]
     └─ max_hooks_per_step: int = 2  # 每步最多触发 2 个钩子

soma/hooks/_builtin_hooks.py  (~80 行)
  6 个内置钩子实例（见下表）

soma/hooks/hooks_config.yaml  (~40 行)
  可热加载的钩子配置，允许零熵研究员调整触发条件

soma/hooks/_reflection_store.py (~60 行)
  └─ ReflectionStore 类  # D1 确认：独立反射存储
     ├─ append(reflection) → None
     ├─ get_recent(user_id, limit) → List[dict]
     └─ clear(user_id) → None
```

**内置钩子定义**：

| 钩子名 | 触发步 | 触发条件 | 提示模板 |
|--------|--------|---------|---------|
| `complexity_awareness` | decompose 后 | `complexity_score > 6` | "这个问题复杂度较高，你第一反应是用哪种经典框架去分析？这个框架是否完全贴合当前场景？" |
| `emotional_load_awareness` | activate 后 | `emotional_load > 0.7` | "我注意到您可能带着较强的情绪参与此次思考。建议先暂停几秒，感受一下呼吸，再决定是否采纳此建议。" |
| `frame_inertia_awareness` | reasoning 后 | 连续 3 轮使用同一主导规律 | "您已连续多次使用「{dominant_law}」角度分析，是否考虑切换到另一个维度？" |
| `info_gap_awareness` | prompt_build 前 | 记忆覆盖度 < 0.3 | "在给出结论前，我注意到还缺少关于当前情境的关键信息，是否先补充后再继续？" |
| `external_dependency_awareness` | action 后 | 建议中外部归因词占比 > 0.6 | "这个建议较多涉及外部因素。在等待外部变化的同时，您自己可以先做的一小步是什么？" |
| `deterministic_awareness` | respond 结束 | 置信度 > 0.9 但历史采纳率 < 0.5 | "我对这个结论的确定性较高，但历史数据显示您的采纳率偏低——这与您的直觉一致吗？" |

**修改文件（2 个）**：

```
soma/config.py (+3 行)
  SOMAConfig 新增字段:
    enable_inner_reflection: bool = False
    hooks_config_path: Optional[str] = None  # 自定义钩子配置

soma/agent.py (+25 行)
  SOMA_Agent.__init__ 中:
    self._hook_registry = None
    if config.enable_inner_reflection:
        self._hook_registry = HookRegistry(...)
  
  respond() 方法中，在 6 个步骤后各插入一行:
    self._emit_hooks("step_name", context_dict)
```

#### 兼容性证明

| 检查项 | 结果 |
|--------|------|
| 现有方法签名是否改变 | 否 |
| respond() 返回值是否改变 | 否 — 钩子提示通过独立的 log channel 输出，不附加到 answer 字符串 |
| 默认行为是否改变 | 否 — `enable_inner_reflection=False`，管道内零额外开销 |
| 钩子异常是否影响管道 | 否 — `_emit_hooks()` 内部所有钩子调用包在 try/except 中 |

**关键设计决策 — 钩子输出的三通道（D1 确认：C+A 混合方案）**：

钩子提示**绝不混入** `respond()` 的返回值。通过三条独立通道输出：

```
Channel 1: soma.reflections (内存 list) — 外部应用主动拉取
  → soma.get_hook_reflections(user_id, limit) → List[dict]
  → 每个 reflection: {step, hook_name, prompt, timestamp, session_id}
  → 外部应用决定是否展示、如何展示给用户

Channel 2: Python logging (INFO 级别) — 静默日志
  → 格式: [HOOK:{hook_name}] step={step} {prompt}
  → 写入日志文件，不打扰用户

Channel 3: reflection_log 表 (SQLite 持久化)
  → reflection_type="inner_hook"
  → 可被 MetaEvolver.evolve() 读取用于权重调整
```

**新增公开方法（不影响存量）**：
```python
# soma/__init__.py 新增
def get_hook_reflections(self, user_id: str = "", limit: int = 10) -> list:
    """获取最近的内求钩子提示，按时间倒序"""
    ...

def clear_hook_reflections(self, user_id: str = "") -> None:
    """清空已读取的钩子提示"""
    ...
```

#### 风险矩阵

| # | 故障场景 | 触发条件 | 概率 | 影响 | 缓解措施 |
|---|---------|---------|------|------|---------|
| H1 | 钩子模板变量缺失导致 KeyError | 配置错误 | 低 | 单个钩子失败 | `str.format()` 用 `{var:default}` 安全格式，缺失变量显示空串 |
| H2 | hooks_config.yaml 格式错误 | 手动编辑失误 | 中 | 自定义钩子加载失败 | YAML 解析异常 → 回退到内置钩子 + WARNING 日志 |
| H3 | 每步触发过多钩子 | 多个钩子条件同时满足 | 低 | 日志/存储膨胀 | `max_hooks_per_step=2`，按优先级截断 |
| H4 | 循环引用（钩子触发导致状态变化→再触发钩子） | 钩子设计不当 | 极低 | 钩子风暴 | 每个钩子每会话最多触发 3 次（去重计数） |
| H5 | `_current_context` 为 None 时钩子触发 | 异常流程 | 低 | 钩子崩溃 | 所有钩子逻辑先检查 `context is not None` |

---

### 模块四：守中执一动态平衡算法

**定位**：`ConsensusProtocol` 的新增策略。不修改现有三条策略（voting/arbitration/synthesis），作为第四条可选策略。

#### 实现内容

**新增文件（1 个）**：

```
soma/multi_agent/zhongdao.py  (~120 行)
  └─ compute_zhongdao_score(candidate_action, user_profile, context) → float
  └─ _estimate_entropy_change(user_profile, action_text) → float
  └─ _extract_external_dependency(action_text) → float
  └─ _compute_growth_alignment(action_text, success_history) → float
```

**修改文件（2 个）**：

```
soma/config.py (+8 行)
  SOMAConfig 新增字段:
    zhongdao_action_weight: float = 0.5      # 行动导向（阳），D3 确认
    zhongdao_awareness_weight: float = 0.5   # 觉察导向（阴），D3 确认
    zhongdao_user_type: str = "general"      # 用户类型: entrepreneur/practitioner/general
    zhongdao_auto_adjust_max: float = 0.02   # 每步最大自动调整幅度，D3 确认
    zhongdao_weight_min: float = 0.3         # 权重硬下限，D3 确认
    zhongdao_weight_max: float = 0.7         # 权重硬上限，D3 确认

soma/multi_agent/consensus.py (+40 行)
  form_consensus() 新增策略分支:
    if strategy == "zhongdao":
        return self._zhongdao_balance(question, opinions)
  
  新增方法:
    def _zhongdao_balance(self, question, opinions) → ConsensusResult
    def _apply_user_type_bias(self) → None         # 用户类型微调，D3 确认
    def _auto_adjust_weights(self, feedback) → None # 自动演化 ±0.02，D3 确认
```

#### 兼容性证明

| 检查项 | 结果 |
|--------|------|
| 现有方法签名是否改变 | 否 — `form_consensus()` 已有 `strategy` 参数，`"zhongdao"` 只是一个新的枚举值 |
| 默认策略是否改变 | 否 — `strategy="auto"` 的逻辑不变 |
| 三个现有策略是否改变 | 否 — `_weighted_voting()`、`_llm_arbitration()`、`_dialectical_synthesis()` 代码不动 |
| 不传 user_profile 时是否崩溃 | 否 — 全部维度使用默认值（0.5） |

#### 阴阳权重自动演化（D3 确认参数）

```python
# 用户类型微调（初始化时一次性调整，±0.1）
USER_TYPE_ADJUSTMENTS = {
    "entrepreneur": {"action": +0.1, "awareness": -0.1},
    "practitioner": {"action": -0.1, "awareness": +0.1},
    "general":       {"action": 0.0, "awareness": 0.0},
}

# 每次 evolve() 调用的自动调整（步进 ±0.02，硬边界 0.3/0.7）
def _auto_adjust_zhongdao(self, recent_feedback: list) -> None:
    avg_feedback = sum(r.score for r in recent_feedback) / len(recent_feedback)
    if avg_feedback > 0.7:
        # 正向反馈 → 强化当前主导方向，但步进极小
        delta = min(0.02, avg_feedback - 0.7)
    elif avg_feedback < 0.3:
        # 负向反馈 → 回调
        delta = -min(0.02, 0.3 - avg_feedback)
    else:
        return  # 中性区间 [0.3, 0.7]，不调整
    
    self.action_weight = clamp(self.action_weight + delta, 0.3, 0.7)
    self.awareness_weight = 1.0 - self.action_weight  # 保持总和恒为 1.0
``` |

#### 风险矩阵

| # | 故障场景 | 触发条件 | 概率 | 影响 | 缓解措施 |
|---|---------|---------|------|------|---------|
| Z1 | `_estimate_entropy_change()` 的 embedder 不可用 | 未配置向量搜索 | 中 | 维度 1 失效 | 回退到字符 bigram 相似度（已有 QualityEvaluator 实现可复用） |
| Z2 | 三个维度权重之和不为 1.0 | 配置错误 | 极低 | 评分偏斜 | `__init__` 时验证权重，自动归一化 |
| Z3 | `success_history` 为空（新用户） | 首次使用 | 高 | 维度 3 失效 | 空列表时 `_compute_growth_alignment` 返回 0.5（中性值） |
| Z4 | 外部依赖关键词列表不完整 | 覆盖不足 | 中 | 维度 2 评分不准 | 关键词列表放在外部 YAML 文件中，可热更新 |
| Z5 | 中道分数与用户实际偏好相反 | 个性化差异 | 中 | 用户不满意 | 结果标注 `strategy_used="zhongdao"`，可通过反馈数据调整权重 |

---

### 模块五：插件系统升级 + 商道罗盘 + 企熵诊断仪

**定位**：将现有插件发现机制升级为可激活、可运行状态机的插件框架，并以两个业务插件作为首批应用。

#### 实现内容

**新增文件（5 个）**：

```
soma/plugin_base.py              (~110 行)
  └─ SomaPlugin(ABC)
     ├─ plugin_id, plugin_name, description
     ├─ should_activate(user_input, context) → bool
     ├─ run(agent, user_input, context) → PluginResult
     └─ reset()  # 重置插件状态
  └─ PluginRegistry
     ├─ discover()
     ├─ register(plugin)
     └─ find_active(user_input, context) → List[SomaPlugin]

soma/plugins/__init__.py

soma/plugins/business_compass.py  (~180 行)
  └─ BusinessCompassPlugin(SomaPlugin)
     三阶段对话状态机: explore_mindset → guided_reflection → insight_summary

soma/plugins/enterprise_entropy.py (~200 行)
  └─ EnterpriseEntropyPlugin(SomaPlugin)
     结构化提问诊断 → 组织熵值计算 → 行动建议生成

soma/plugins/plugin_schema.yaml   (~20 行)
```

**修改文件（3 个）**：

```
soma/config.py (+3 行)
  SOMAConfig 新增字段:
    enable_plugins: bool = False
    plugin_dir: Optional[str] = None

soma/plugin.py (+15 行)
  新增: PluginRegistry 类 + register_plugin() / activate_plugin() 函数

soma/__init__.py (+20 行)
  SOMA 新增方法:
    def enable_plugin(self, plugin_id: str) → bool
    def disable_plugin(self, plugin_id: str) → bool
    def list_plugins(self) → list
```

#### 兼容性证明

| 检查项 | 结果 |
|--------|------|
| 现有方法是否改变 | 否 — 新增 `enable_plugin()` / `disable_plugin()` / `list_plugins()` |
| 默认行为是否改变 | 否 — `enable_plugins=False`，插件系统完全不加载 |
| 插件异常是否阻塞管道 | 否 — 插件在 `respond()` 入口处检查，异常时跳过插件走正常管道 |
| 插件数据表是否冲突 | 否 — 插件数据表统一前缀 `plugin_{plugin_id}_` |

**关键设计 — 插件拦截与回退**：

```python
# SOMA.respond() 入口
def respond(self, problem: str, user_id: str = "") -> str:
    # 插件拦截 (v0.9.1)
    if self._config.enable_plugins:
        try:
            for plugin in self._plugin_registry.find_active(problem, context):
                result = plugin.run(self._agent, problem, context)
                if result.handled:
                    return result.response
        except Exception:
            _log.warning("插件执行异常，回退到正常推理管道", exc_info=True)
    
    # 正常推理管道（代码不变）
    return self._agent.respond(problem, user_id=user_id)
```

#### 风险矩阵

| # | 故障场景 | 触发条件 | 概率 | 影响 | 缓解措施 |
|---|---------|---------|------|------|---------|
| P1 | 插件状态机卡住 | 用户中途退出或输入异常 | 中 | 后续对话异常 | 状态机有 10 分钟超时，超时自动 `reset()` |
| P2 | 插件误触发（非商业问题触发商道插件） | 关键词匹配过宽 | 中 | 体验干扰 | 触发器保守设计：商业关键词 + 用户问题长度 > 10 字 |
| P3 | 插件数据库无限增长 | 长期使用 | 低 | 存储压力 | 插件会话记录保留最近 100 条 |
| P4 | 插件文件热加载冲突 | 运行时替换插件文件 | 极低 | 插件加载失败 | 插件在 SOMA 初始化时一次性加载，运行时不热加载 |
| P5 | 两个插件同时激活冲突 | 用户问题同时命中两个插件 | 低 | 输出混乱 | `find_active()` 只返回优先级最高的 1 个插件 |

---

### 模块六：规律贡献接口

**定位**：独立的贡献管理系统。不与核心推理管道耦合，只通过审核流程向规律网络注入已验证的思维模型。

#### 实现内容

**新增文件（3 个）**：

```
soma/contribution.py             (~200 行)
  └─ PatternSubmission 数据类
  └─ ContributionHandler
     ├─ submit(submission) → submission_id
     ├─ auto_review(submission_id) → ReviewResult
     ├─ manual_approve(submission_id, reviewer_id) → bool
     ├─ record_adoption(pattern_id, outcome) → None
     └─ get_active_patterns(context, limit) → list

soma/contribution_schema.json    (~50 行)
  提交格式 JSON Schema

soma/contribution_store.py      (~100 行)
  └─ ContributionStore (SQLite)
     表: contributions, approved_patterns, adoption_log
```

**修改文件（2 个）**：

```
soma/engine.py (+10 行)
  WisdomEngine 新增方法:
    def inject_contributed_pattern(pattern) → bool
    在 decompose() 的 fallback 阶段，检查是否有匹配的贡献规律

soma/law_discovery.py (+15 行)
  LawDiscovery 新增:
    def validate_contribution(submission) → dict  # 去重 + 格式校验
```

#### 兼容性证明

| 检查项 | 结果 |
|--------|------|
| 现有方法是否改变 | 否 — `decompose()` 内部在 fallback 阶段追加贡献规律检查，只增加候选不删除现有逻辑 |
| 贡献规律是否可能破坏现有推理 | 否 — 贡献规律只在 fallback 阶段参与，优先级低于内置 7 条规律 |
| 未审核的贡献是否影响系统 | 否 — 只有 `manual_approve()` 通过后才注入规律网络 |
| 贡献存储是否与核心数据库冲突 | 否 — 独立的 `contribution_store.db` 文件 |

#### 审核流程与安全设计（D4 + D5 确认）

**第一关：自动审核（4 类禁止内容，直接拒绝）**

| 类别 | 检测方式 | 示例 |
|------|---------|------|
| 投机取巧 | 关键词匹配（走捷径/薅羊毛/套利/钻空子） | "如何利用规则漏洞快速获利" |
| 损害他人 | 关键词匹配（攻击/诋毁/榨取/欺骗） | "怎样让竞争对手倒闭" |
| 违背内求 | 语义判断（外部归因占比 > 0.8 + 零自省词汇） | "一切都是环境的问题" |
| 捷径成功论 | 关键词+语义（速成/秘籍/不劳而获） | "三天成为亿万富翁的秘诀" |

**第二关：5 柱接受量表（人工审核，每柱 0-20 分，总分 ≥ 70 分通过）**

| 柱名 | 含义 | 满分标准 |
|------|------|---------|
| 善行 | 是否引向正向行动 | 提供具体、可操作的向善行动路径 |
| 善人 | 是否促进人的成长 | 关注人的内在成长而非仅外部结果 |
| 善心 | 是否引导内求觉察 | 包含自我审视和反思的触发点 |
| 善史 | 是否符合长期价值 | 不追求短期效果，考虑长期影响 |
| 善修 | 是否可实践验证 | 规律可以被个人实践检验，非纯理论 |

审核标准书面文档路径：`docs/contribution-audit-standards.md`

```
用户提交 contribution.json
         │
         ▼
  ┌──────────────┐
  │ 自动审核      │ ← 4 类禁止检测 + JSON Schema + 去重 (sim < 0.85)
  │               │   任一禁止类命中 → 直接拒绝，附拒绝原因
  └──────┬───────┘
         │ auto_review 通过
         ▼
  ┌──────────────┐
  │ 人工审核队列   │ ← 认证「规律审核员」按 5 柱量表评分
  │               │   总分 ≥ 70 → 通过 | 50-69 → 退回修改 | < 50 → 拒绝
  └──────┬───────┘
         │ manual_approve 通过
         ▼
  ┌──────────────┐
  │ 纳入规律网络   │ ← pattern_id 加入向量索引，初始 energy=5
  │ v1.0 (活跃)   │   触发条件加入 engine 关键词表
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │ 运行中追踪     │ ← 每次采纳 +1 energy，负面反馈 -2 energy
  │               │   energy < 0 → 自动降级（归档，不删除）
  └──────┬───────┘
         │ energy ≥ 50 或 >30 次采纳（D5 确认）
         ▼
  ┌──────────────┐
  │ 升级审核       │ ← 3 位独立审核员全部通过 → v2.0
  │               │   旧版 v1.0 归档到 pattern_archive 表
  │               │   需要 changelog（变更说明 + 理由）
  └──────────────┘
```

#### 风险矩阵

| # | 故障场景 | 触发条件 | 概率 | 影响 | 缓解措施 |
|---|---------|---------|------|------|---------|
| C1 | 恶意提交（不当内容） | 开放提交 | 中 | 审核队列污染 | 自动审核 4 类禁止检测（D4）+ 人工为最后关卡 |
| C2 | 提交大量低质量规律 | 滥用 | 中 | 审核人力消耗 | 每人每天限提交 3 条；energy < -5 的提交者暂停提交权限 |
| C3 | 贡献规律与现有规律高度相似 | 独立发现相同规律 | 中 | 冗余 | embedding sim > 0.85 → 拒绝，提示「与已有规律高度相似」 |
| C4 | 5 柱评分标准执行不一致 | 不同审核员理解偏差 | 中 | 审核质量波动 | 编写 `contribution-audit-standards.md`（D4）作为统一标准 + 审核员培训 |
| C5 | 升级审核 3 人难以凑齐 | 审核员活跃度不足 | 中 | 升级流程阻塞 | 升级不紧急（energy < 50 时规律正常使用），审核超时 7 天自动提醒 |
| C6 | 旧版本归档后无法回溯 | SQLite 文件损坏 | 极低 | 历史数据丢失 | WAL 模式 + 定期备份 + pattern_archive 表与主表同目录 |

---

## 五、端到端风险推演

### 5.1 升级场景推演

**场景 A：老用户从 v0.9.0 升级到 v0.9.1**

```
1. pip install --upgrade soma-wisdom==0.9.1
2. 原有代码 import soma; soma = SOMA(use_vector_search=True)
3. 不修改任何代码，直接运行
4. 预期：行为与 v0.9.0 完全一致（所有新 flag 默认 False）
5. 新文件 soma/entropy.py 等被安装但不会被 import（延迟加载）
6. analytics_sessions 表追加 entropy_score 列（ALTER TABLE，瞬间完成）
```

**场景 B：新用户直接安装 v0.9.1**

```
1. pip install soma-wisdom==0.9.1
2. 默认行为：纯 v0.9.0 模式（所有新功能关闭）
3. 阅读文档后按需开启：
   soma = SOMA(use_vector_search=True, enable_cognitive_entropy=True)
```

**场景 C：用户开启部分新功能**

```
1. 只开熵值仪表盘，不开钩子/框架检测/插件
2. 预期：仪表盘正常工作，管道不受影响
3. 验证：每个模块的独立开关有效
```

### 5.2 数据库迁移推演

所有新增列/表都使用 `IF NOT EXISTS` 或 try/except 包围：

```python
# 安全迁移模式 (每个新增列都用这个模式)
for col, col_def in [
    ("entropy_score", "REAL DEFAULT NULL"),
]:
    try:
        self._conn.execute(
            f"ALTER TABLE analytics_sessions ADD COLUMN {col} {col_def}"
        )
    except sqlite3.OperationalError:
        pass  # 列已存在，跳过
```

这个模式已在 v0.9.0 的 `episodic.py` 中验证过（88-100 行的列迁移逻辑），稳定可靠。

### 5.3 跨模块交互推演

六个模块之间的依赖关系：

```
               ┌─────────────┐
               │ 认知熵值仪表盘 │  ← 完全独立，零耦合
               └─────────────┘
               
               ┌─────────────┐
               │ 空性偏差检测   │  ← 依赖: hub/_core.py (追加式)
               └──────┬──────┘
                      │ 共享: recent_conversation_turns
                      │
               ┌──────▼──────┐
               │ 内求复盘钩子   │  ← 依赖: agent.py 管道 (观察者)
               └──────┬──────┘
                      │ 同一个 _emit_hooks() 调用点
                      │
      ┌───────────────┼───────────────┐
      │               │               │
┌─────▼─────┐  ┌──────▼──────┐  ┌─────▼─────┐
│ 守中执一    │  │ 商道罗盘     │  │ 规律贡献   │
│ (独立策略)  │  │ (独立插件)   │  │ (独立模块) │
└───────────┘  └─────────────┘  └───────────┘
```

关键发现：**六个模块之间几乎零相互依赖**。这意味着：
1. 任何一个模块出问题，不会牵连其他模块
2. 可以独立回退任何一个模块
3. 可以分批发版，不需要六个一起上

### 5.4 最坏情况推演

**假设：升级后出现严重 bug，需要紧急回退**

```bash
# 回退方案：一行命令
pip install soma-wisdom==0.9.0

# 影响评估：
# - 数据库中的新列（entropy_score 等）留在表中，不影响 v0.9.0 读取
# - 新增文件（entropy.py, hooks/ 等）被卸载
# - 用户代码不需要任何修改
# 回退时间：< 1 分钟
```

---

## 六、对整体开发计划的影响

### 原计划

```
v0.9.x — 多Agent协作（已完成）
v0.10.0 — Agent自动孵化 + 跨Agent知识蒸馏 + Agent市场协议 + 联邦演化
```

### 调整后

```
v0.9.1 — 零熵觉察层（本次整合）
  ├─ 认知熵值仪表盘
  ├─ 空性偏差检测
  ├─ 内求复盘钩子
  ├─ 守中执一算法
  ├─ 商道罗盘 + 企熵诊断仪（插件）
  └─ 规律贡献接口

v0.10.0 — Agent生态扩展（原计划，不变）
  ├─ Agent自动孵化
  ├─ 跨Agent知识蒸馏
  ├─ Agent市场协议
  └─ 联邦演化
```

### 为什么 v0.9.1 不影响 v0.10.0

1. **代码路径不重叠**：v0.9.1 的新代码在 `soma/entropy/`、`soma/hooks/`、`soma/plugins/`、`soma/contribution.py`，v0.10.0 的改动在 `soma/multi_agent/`，互不冲突。

2. **v0.9.1 的插件框架对 v0.10.0 有益**：v0.10.0 的 Agent 市场协议可以直接复用插件注册和激活机制。

3. **v0.9.1 的贡献接口对 v0.10.0 有益**：跨 Agent 知识蒸馏可以利用贡献规律作为蒸馏源。

4. **开发资源可以并行**：如果有多人协作，v0.9.1 和 v0.10.0 可以同时推进。

---

## 七、实施路线图（含兼容性验证关卡）

### 阶段划分（按文化赋能师建议的优先级调整）

```
Phase A: 审核标准 + 空性偏差（第1-2周）[D4 优先]
  ├─ 编写 docs/contribution-audit-standards.md（4类禁止+5柱评分细则）
  └─ 模块二: 空性偏差检测升级（FrameAnchoringDetector）
  验收: 审核标准文档完成 + 框架检测不影响存量测试

Phase B: 钩子反射 + 阴阳权重（第3-4周）[D1 + D3 并行]
  ├─ 模块三: 内求复盘钩子（三通道输出 + get_hook_reflections API）
  └─ 模块四: 守中执一算法（用户类型微调 + 自动演化 ±0.02）
  验收: 钩子框架 + 6内置钩子 + 异常隔离 + 中道评分三维度

Phase C: 熵值引导模式（第5-6周）[D2，依赖 Phase B 钩子框架]
  └─ 模块一: 认知熵值仪表盘 + 引导模式状态机（40/50 阈值）
  验收: 熵值三维度计算 + 引导模式进入/退出正常

Phase D: 生态扩展层（第7-10周）[D5 + D6 收尾]
  ├─ 模块五: 插件系统 + 商道罗盘 + 企熵诊断仪
  ├─ 模块六: 规律贡献接口（4类禁止 + 5柱评分 + 版本升级管理）
  └─ D6: 零熵测试系统独立并行文档说明
  验收: 端到端贡献→审核→纳入→升级→归档流程
```

### 每阶段的兼容性验证关卡

每个 Phase 结束时必须通过以下关卡才能进入下一阶段：

```
关卡 1: 存量测试全通过
  运行: pytest tests/ -v
  要求: 473 项全绿，0 失败

关卡 2: API 签名一致性
  运行: 脚本逐一调用 30 个公开接口，确认签名和返回类型不变

关卡 3: 默认行为一致性
  验证: SOMA() 无参构造后 respond/chat/remember 行为与 v0.9.0 相同

关卡 4: 独立开关验证
  验证: 逐一开启每个新 flag，确认只有目标模块生效

关卡 5: 升级回退验证
  验证: pip install soma-wisdom==0.9.0 后数据库正常读写
```

### 测试增量估算

| Phase | 新增测试数 | 覆盖内容 |
|-------|----------|---------|
| Phase A | ~15 项 | 框架检测、审核格式校验、4类禁止检测、5柱评分计算、降级路径 |
| Phase B | ~25 项 | 钩子注册/触发/频率限制/异常隔离/三通道输出/反射API、中道评分三维度/用户类型微调/自动演化边界 |
| Phase C | ~15 项 | 熵值三维度计算、引导模式进入/退出/呼吸提醒、降级路径 |
| Phase D | ~30 项 | 插件发现/激活/状态机/拦截回退、贡献提交/4类禁止/5柱评分/去重/版本升级/归档 |
| **总计** | **~85 项** | **累计测试约 558 项** |

---

## 八、总结

### 这次整合的本质

不是给 SOMA 添加六个「功能」。而是为其注入一个**新的认知操作系统**。

当前 SOMA 的操作系统是「问题解决模式」——接收问题→拆解→检索→推理→回答。这次整合后，SOMA 将获得第二个模式——「觉察辅助模式」——在解决问题的同时，温和地帮助使用者看见自己的思维惯性、情绪扰动、认知边界。

### 决策状态

6 项关键决策（D1-D6）已经与文化赋能师确认，详见第九节。核心决策：
- **D1**：钩子走独立反射 API，不混入 answer 文本
- **D2**：连续 3 轮 < 40 分触发引导模式，2 轮 ≥ 50 分退出
- **D3**：阴阳权重 0.5/0.5 起，用户类型 ±0.1，自动演化 ±0.02，边界 0.3/0.7
- **D4**：4 类禁止 + 5 柱量表，需编写书面审核标准
- **D5**：高能量规律（≥50 或 >30 次采纳）需 3 人审核，旧版归档不删除
- **D6**：SOMA 熵值与零熵测试系统独立并行，v0.9.1 不做技术集成

### 技术上的承诺

| 承诺 | 如何保证 |
|------|---------|
| 不破坏一行存量代码 | 所有新功能默认关闭，新行为由独立 config flag 控制 |
| 不改变任何一个 API | 30 个公开接口签名和返回类型不变 |
| 出问题可以一键回退 | pip install 旧版本，数据库无损 |
| 六个模块独立可开关 | 每个模块有独立的 enable_xxx config flag |

## 九、六项关键决策（已确认）

以下 6 项决策经与文化赋能师深入讨论后已确定，作为模块实现的最终技术参数。

---

### D1：内求复盘钩子的输出通道

**决策：C+A 混合方案 — 独立钩子反射 API + 静默日志**

钩子提示**绝不混入** `respond()` 的返回值。技术实现：

```
钩子触发后的三条输出通道：
┌─────────────────────────────────────────────────────┐
│ Channel 1: soma.reflections (内存 list)              │
│   → soma.get_hook_reflections(user_id) → List[dict] │
│   → 外部应用主动拉取，按需展示给用户                   │
│   → 每个 reflection 包含: step, hook_name, prompt,   │
│      timestamp, session_id                          │
├─────────────────────────────────────────────────────┤
│ Channel 2: Python logging (级别 INFO)                │
│   → 静默写入日志文件，不打扰用户                       │
│   → 格式: [HOOK:{hook_name}] step={step} {prompt}   │
├─────────────────────────────────────────────────────┤
│ Channel 3: reflection_log 表 (持久化)                 │
│   → reflection_type="inner_hook"                    │
│   → 可被 MetaEvolver 的 evolve() 读取用于权重调整      │
└─────────────────────────────────────────────────────┘
```

新增公开方法（不影响存量）：

```python
def get_hook_reflections(self, user_id: str = "", limit: int = 10) -> list:
    """获取最近的内求钩子提示，按时间倒序"""
    ...

def clear_hook_reflections(self, user_id: str = "") -> None:
    """清空已读取的钩子提示"""
    ...
```

**设计理由**：钩子提示是「觉察辅助」而非「答案修正」。强行混入 answer 会破坏返回格式兼容性，让外部应用自行决定如何展示是最灵活的做法。

---

### D2：认知熵值的干预阈值

**决策：连续 3 轮 < 40 分触发引导模式，连续 2 轮 ≥ 50 分后退出**

引导模式的行为变更：

| 正常模式 | 引导模式 |
|---------|---------|
| 直接回答占 70%+ | 直接回答降至 < 30% |
| 主动给出结论 | 以引导性问题为主（「你觉得…？」「如果换个角度…？」） |
| 无干预 | 响应末尾追加呼吸提醒（随机从 3 条模板中选取） |

技术参数：

```python
GUIDANCE_ENTER_THRESHOLD = 40      # 熵值低于 40 分
GUIDANCE_ENTER_CONSECUTIVE = 3     # 连续 3 轮
GUIDANCE_EXIT_THRESHOLD = 50       # 熵值恢复到 50 分以上
GUIDANCE_EXIT_CONSECUTIVE = 2      # 连续 2 轮
GUIDANCE_DIRECT_ANSWER_RATIO = 0.3 # 引导模式下直接回答占比上限

# 呼吸提醒模板（内置 3 条，可扩展）
BREATHING_REMINDERS = [
    "在继续之前，不妨先做一次深呼吸，感受当下的状态。",
    "暂停三秒，问问自己：我现在的情绪在哪个位置？",
    "注意到思绪的速度了吗？先回到呼吸，再回到问题。",
]
```

引导模式状态存储在 `CognitiveEntropyCalculator` 的内存状态中，不持久化（会话级别的临时状态）。

---

### D3：守中执一算法的阴阳权重

**决策：初始 0.5/0.5，按用户类型微调，步进 ±0.02，硬边界 0.3/0.7**

```python
# 初始权重
zhongdao_action_weight: float = 0.5     # 行动导向（阳）
zhongdao_awareness_weight: float = 0.5  # 觉察导向（阴）

# 用户类型微调（初始化时一次性调整）
USER_TYPE_ADJUSTMENTS = {
    "entrepreneur": {"action": +0.1, "awareness": -0.1},  # 创业者 → 偏行动
    "practitioner": {"action": -0.1, "awareness": +0.1},  # 修行者 → 偏觉察
    "general":       {"action": 0.0, "awareness": 0.0},   # 通用 → 不调整
}

# 自动演化的约束
AUTO_ADJUST_MAX_PER_STEP = 0.02   # 每步最多调整 ±0.02
HARD_MIN = 0.3                     # 硬下限
HARD_MAX = 0.7                     # 硬上限

# 自动调整逻辑
def _auto_adjust_zhongdao(self, feedback_score: float):
    """根据用户反馈微调权重"""
    if feedback_score > 0.7:  # 正向反馈 → 微调当前主导权重
        delta = min(AUTO_ADJUST_MAX_PER_STEP, feedback_score - 0.7)
    elif feedback_score < 0.3:  # 负向反馈 → 回调
        delta = -min(AUTO_ADJUST_MAX_PER_STEP, 0.3 - feedback_score)
    else:
        return  # 中性反馈，不调整
    
    new_action = clamp(self.action_weight + delta, HARD_MIN, HARD_MAX)
    new_awareness = 1.0 - new_action  # 保持总和为 1.0
    self.action_weight = new_action
    self.awareness_weight = new_awareness
```

**设计理由**：0.5 起点体现「守中」原则 — 不预设行动或觉察哪个更重要。用户类型只做微调（±0.1），不会让初始状态偏离太远。自动演化的步进极小（±0.02），确保权重变化缓慢而稳定。

---

### D4：规律贡献的审核标准

**决策：4 类禁止内容 + 5 柱接受量表 + 需编写书面审核标准文档**

**4 类禁止内容（自动审核阶段直接拒绝）**：

| 类别 | 检测方式 | 示例 |
|------|---------|------|
| 投机取巧 | 关键词匹配（走捷径/薅羊毛/套利/钻空子） | "如何利用规则漏洞快速获利" |
| 损害他人 | 关键词匹配（攻击/诋毁/榨取/欺骗） | "怎样让竞争对手倒闭" |
| 违背内求 | 语义判断（外部归因占比 > 0.8 + 零自省词汇） | "一切都是环境的问题，只要换环境就好" |
| 捷径成功论 | 关键词+语义（速成/秘籍/不劳而获） | "三天成为亿万富翁的秘诀" |

**5 柱接受量表**（人工审核阶段的评分标准，每柱 0-20 分，总分 ≥ 70 分通过）：

| 柱名 | 含义 | 满分标准 |
|------|------|---------|
| 善行 | 是否引向正向行动 | 提供具体、可操作的向善行动路径 |
| 善人 | 是否促进人的成长 | 关注人的内在成长而非仅外部结果 |
| 善心 | 是否引导内求觉察 | 包含自我审视和反思的触发点 |
| 善史 | 是否符合长期价值 | 不追求短期效果，考虑长期影响 |
| 善修 | 是否可实践验证 | 规律可以被个人实践检验，非纯理论 |

**需编写文档**：`docs/contribution-audit-standards.md`，详细记录审核流程、标准操作程序、边界案例判定指南。该文档作为规律审核员的培训材料和操作手册。

---

### D5：规律升级的版本管理

**决策：版本化升级，高能量规律需 3 人独立审核**

```
升级路径：
┌──────────┐    energy ≥ 50    ┌──────────────┐
│  v1.0    │ ──── 或 ───────→  │  升级审核流程  │
│ (活跃)   │  >30次采纳        │  3人独立审核   │
└──────────┘                   └──────┬───────┘
                                      │ 全部通过
                                      ▼
                               ┌──────────────┐
                               │  v2.0        │
                               │ (升级生效)    │
                               │ changelog    │
                               └──────────────┘

旧版本处理：
- v1.0 → 归档到 contribution_store.db 的 pattern_archive 表
- 不删除（保持完整历史链）
- 归档规律仍可查询但不参与活跃匹配
- 如果 v2.0 的 energy 降到 < 0，可回退到 v1.0
```

**Changelog 格式要求**：
```json
{
  "pattern_id": "P_xxx",
  "from_version": "1.0",
  "to_version": "2.0",
  "reviewers": ["reviewer_id_1", "reviewer_id_2", "reviewer_id_3"],
  "change_summary": "修改了触发条件的范围，从仅限企业场景扩展到团队场景",
  "rationale": "在零熵智库的 50+ 次采纳中发现该规律在团队层面同样有效",
  "timestamp": "2026-05-12T10:00:00Z"
}
```

---

### D6：与零熵测试系统的关系

**决策：独立并行，v0.9.1 不做技术集成**

```
当前关系：
┌─────────────┐         ┌──────────────────┐
│ SOMA 熵值    │         │ 零熵智库测试系统    │
│ (内部指标)   │         │ (独立外部系统)     │
│ - 逻辑连贯性  │  无技术  │ - 20项成长指标    │
│ - 情绪稳定性  │  集成   │ - 生命赋能评估    │
│ - 焦点清晰度  │         │ - 商业实践体系    │
└─────────────┘         └──────────────────┘
        │                        │
        │     ┌──────────────┐   │
        └─────┤ 人工对照参考   ├───┘
              │ (不在代码层面) │
              └──────────────┘
```

**v0.9.1 的做法**：
- SOMA 的 `CognitiveEntropyCalculator` 和零熵测试系统各自独立运行
- 不做 API 对接、数据交换或分数映射
- 两者产出可以人工对照参考，但不做技术层面的关联
- 未来版本（v0.10.0+）如需集成，届时再设计数据交换协议

**设计理由**：两个系统的评估维度和方法论不同，强行在代码层面对齐会引入不必要的耦合。保持独立让各自可以按自己的节奏迭代。

---

### 决策后的实施优先级调整

文化赋能师建议的实施顺序：

```
优先级 1: D4 — 编写审核标准文档（代码实现的前提）
    ↓
优先级 2: D1 + D3 — 钩子反射 API + 阴阳权重配置（管道层改动）
    ↓
优先级 3: D2 — 熵值引导模式（依赖 D1 的钩子框架就位）
    ↓
优先级 4: D5 + D6 — 版本管理 + 系统关系文档（收尾）
```

对应到模块实现的调整：

| 原 Phase | 调整后 | 变更说明 |
|----------|--------|---------|
| Phase A: 熵值仪表盘 + 空性偏差 | Phase A: 空性偏差 + 审核标准文档(D4) | 先写标准再写代码 |
| Phase B: 内求复盘钩子 | Phase B: 内求钩子(D1) + 阴阳权重(D3) | 钩子输出通道独立化 + 权重微调逻辑 |
| Phase C: 守中执一算法 | Phase C: 认知熵值 + 引导模式(D2) | 熵值干预阈值 + 引导模式状态机 |
| Phase D: 插件 + 贡献 | Phase D: 插件 + 贡献(D5) + 系统文档(D6) | 版本管理 + 独立并行说明 |

---

## 十、下一步

1. **编写审核标准文档**：创建 `docs/contribution-audit-standards.md`，详细定义 4 类禁止内容的判定边界和 5 柱评分细则（与 D4 对齐）
2. **更新模块实现细节**：按 D1-D6 的确认参数细化各模块的伪代码和测试用例
3. **启动 Phase A**：空性偏差检测 + 审核标准文档编写
4. **同步筹备 v0.10.0**：Agent 生态扩展计划继续推进，与 v0.9.1 并行

---

*文档版本: 3.0 | 适用版本: SOMA v0.9.0 → v0.9.1 | 2026-05-12 | 纳入 D1-D6 确认决策 + 实施优先级调整*
